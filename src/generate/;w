#include "generate/group.h"

size_t *
put_groups_linear(size_t *restrict group,
		  const size_t row_count,
		  const size_t group_count)
{
	const double slope = ((double) (row_count * 2lu))
			   / ((double) group_count);

	double dbl_acc  = 0.0;
	size_t size_acc = 0;


	const size_t *const restrict until = group + group_count;

	do {

	} while (group < until);

	return until;
}
